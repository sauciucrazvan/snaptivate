import 'package:flutter/material.dart';

class Snaptivate extends StatelessWidget {
  const Snaptivate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
